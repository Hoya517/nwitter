import { HashRuter as Router, Route, Switch } from "react-router-dom";

const AppRouter = () => {
    retrurn (
        <Router>
            <Switch>
                <Route />
            </Switch>
        </Router>
    );
};

export default AppRouter;